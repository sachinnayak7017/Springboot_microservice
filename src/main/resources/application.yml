server:
  port: 8080

spring:
  application:
    name: userservice
  datasource:
    url: jdbc:postgresql://localhost:5432/Fitness-micro-user
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
